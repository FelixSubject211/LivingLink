services:
  app:
    build: .
    container_name: livinglink-app
    restart: unless-stopped
    expose:
      - "3000"
      - "3001"
    env_file:
      - .env
    volumes:
      - ./groups.json:/config/groups.json:ro
    depends_on:
      - mongo